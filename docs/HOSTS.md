# Hosts

This plugin is a Claude Code marketplace (`/plugin marketplace add SpillwaveSolutions/image_gen`).

Grok Build loads Claude plugins zero-config. Codex reads `.codex-plugin/plugin.json` and `.agents/plugins/marketplace.json`. Cursor reads `.cursor-plugin/plugin.json` and `.cursor/rules/`.

Universal install: `skilz install SpillwaveSolutions/image_gen` (Agent Plugins v1 `plugin.json`).

WikiTicket SDD: see WORKLOG.md.
